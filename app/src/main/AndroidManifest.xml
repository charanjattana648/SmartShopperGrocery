<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smartshopper.smartshoppergrocery">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeScreenMain" />
        <activity android:name=".FruitsCategoryActivity" />
        <activity android:name=".VegetablesCategoryActivity" />
        <activity android:name=".BakeryCategoryActivity" />
        <activity android:name=".CandyCategoryActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ForgetResetPasswordActivity" />
        <activity android:name=".SelectedGridItem" />
        <activity android:name=".ViewCartActivity"></activity>
        <activity android:name=".PurchaseRecordActivity"></activity>
    </application>

</manifest>